[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["external", ".*", "build", "dist", "venv", ".venv"]
addopts = "-ra"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
